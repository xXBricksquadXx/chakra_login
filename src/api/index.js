import auth from './auth';

const api = {
  show(email, password) {
    console.log(auth);
  },

  create(email, password) {
    console.log(auth);
  },

  update(email) {
    console.log(auth);
  },
};

export default api;
